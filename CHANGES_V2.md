# 🎰 Casino — что нового (V2)

## ✅ Что сделано в этом архиве

### 1. Биржа
- Кнопка **«Продати ВСЕ»** в портфеле
- Шанс роста увеличен (`-0.49` → `-0.46` в формуле тика)

### 2. Ракета
- **Авто-вивід** при заданном множителе (чекбокс + поле + пресеты x1.5/x2/x3/x5/x10)

### 3. Слоты — ребаланс
- Тройка: 5% → **3%**
- Пара: 35% → **25%**
- Проигрыш: 60% → **72%**

### 4. Рефералка
- При регистрации можно ввести **реф-код** (или зайти по `?ref=CODE`)
- Реферер сразу получает **+1200 CR**
- У каждого юзера автогенерится `referral_code`

### 5. Трейд
- Новая страница `/trade` — P2P обмен NFT + CR
- Комиссия **5%** с отправителя
- Входящие/исходящие запросы, accept/decline

### 6. Инвентарь
- Кнопка **«Продати ВСЕ»** (по 70% от цены)

### 7. Профиль
- Кнопка профиля уже была в шапке + добавлена в нижнюю навигацию

### 8. Кейсы (6 шт)
- Таблица `cases` с 6 кейсами от 100 до 250000 CR создается миграцией
- ⚠️ UI кейсов пока тот же (1 кейс) — нужно ещё расширить `Cases.tsx` чтобы выбирать из 6
- Можно добавить вторым проходом

### 9. Редкая валюта (RC)
- Таблица `rare_currency` (всего 500 на всю игру) + функция `mint_rare()`
- Поле `users.rare_balance`
- ⚠️ UI выдачи/обмена пока не подключен — инфраструктура готова

---

## ⚠️ ОБЯЗАТЕЛЬНО: запусти SQL

В Supabase SQL Editor запусти **`MIGRATION_V2.sql`** (целиком).
Это создаст:
- поля `favorites`, `referral_code`, `referred_by`, `rare_balance` в `users`
- таблицы `cases`, `trades`, `referrals`, `rare_currency`
- функцию `mint_rare()`

Без этой миграции ничего не заработает.

---

## 🛡️ Анти-абуз (как ты и просил админ-замечания)

Сейчас рефка выдается **сразу при регистрации** — это будут фармить фейками.
Минимум что стоит добавить позже:
- IP-чек на дубли (требует серверной ручки)
- Триггер «друг должен сделать N ставок» вместо моментальной выдачи

---

## 📌 Что НЕ влезло (нужен ещё проход)

- UI выбора 1 из 6 кейсов в `Cases.tsx` (таблица в БД готова)
- Избранное в инвентаре/профиле (поле `users.favorites` готово)
- Полный обмен RC между игроками (БД готова, UI нет)

Скажи — добью вторым архивом.
